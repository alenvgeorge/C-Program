/**
	* @file string.h
	*/
#ifndef __STRING_H__
#define __STRING_H__

/**
	* Splits a string into substring based on delimeter
	* @param[in] string
	* @return Substring by removing the delimeter
	* @note The string is split whenever delimeter '_' is present.
	*/
int delimeter(char string[ ]);

#endif
